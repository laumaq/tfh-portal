{
  "name": "tfh-backup-scripts",
  "version": "1.0.0",
  "description": "Scripts de backup pour TFH Portal",
  "main": "backup.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backup": "node backup.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "@supabase/supabase-js": "^2.39.0",
    "node-fetch": "^2.6.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
